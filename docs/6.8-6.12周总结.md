
**本周工作总结（06/08 - 06/12）**


工程侧完成了 DeepSeek provider 接入，同时将 eval runner 重构为支持多线程并发、RPM 限流和线程安全 trace 写入.

实验侧补齐了 Phase 4/5 的分析链路，新增聚合、统计分析、图表生成和自动报告脚本.

质量保障方面，本周新增了多组单元测试和回归测试，覆盖 metrics、judge、interactive runner、hierarchical action filtering 等关键路径。

**具体改动**

1. **修复评测正确性**
   - 修复 hierarchical tools 下 action filtering 不生效的问题，状态机可达 action 现在能正确传入 tool schema。
   - `metrics` 增强：支持 `key_fields` 生成 ID 容错、`OUT_OF_SCOPE` 检测、`task_success_rate`、cascade failure、resource-query-before-action 等指标。


2. **接入真实模型与并发执行**
   - 新增 DeepSeek provider，兼容 OpenAI-style API。
   - 修复 DeepSeek 等严格 provider 对 tool/user message 顺序、top-level JSON schema 的校验问题。
   - `eval.runner` 支持 `--workers` 并发、`--rate-limit` RPM 限流，并给 tracer 增加写锁，避免多线程写 trace 时损坏文件。


3. **扩展 SCADA 工具面**
   - 新增并注册一批领域工具, 用于后续实验
   - 更新 registry 和 `tools/__init__.py`，让新增工具进入默认工具注册流程。

4. **实验分析准备**
   - 新增 `aggregate.py`、`analyze.py`、`make_report.py`、`generate_notebooks.py` 以及 phase4 批量运行脚本。
   - 生成主结果 notebook、ablation notebook、论文图表和 `EXPERIMENT_REPORT.md`。

5. **依赖与测试**
   - `pyproject.toml` 新增 `openai` 依赖。
   - 新增/扩展测试覆盖：metrics、judge、hierarchical action filtering、interactive runner、tracer、registry dispatcher 等。


