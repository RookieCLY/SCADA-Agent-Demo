# xiaomi-mimo 手动测试指南

## 0. 前置条件

```bash
cd E:/Agent/Demo
```

- `venv/` 已存在，里面带 `openai`、`pydantic`、`pyyaml`、`loguru`、`python-dotenv` 等
- `.env` 已经放在仓库根目录（与 `.env.example` 内容一致，含 `XIAOMI-MIMO_API_KEY` / `_API_URL` / `_MODEL`）
- Windows Git Bash 下加 `PYTHONIOENCODING=utf-8` 可避免中文乱码

后面命令统一以 `venv/Scripts/python` 调用。

---

## 1. 涉及的文件总览

### 1.1 真实 LLM 相关（这次新增 / 修改）

| 文件 | 角色 |
|---|---|
| `agent/config.py` | `ModelConfig.provider` 的 `Literal` 增加 `"xiaomi-mimo"` |
| `agent/llm.py` | 新增 `OpenAICompatibleLLM` 适配器 + `_load_dotenv_into_environ()`；扩展 `build_llm()` 工厂支持 `xiaomi-mimo` |
| `agent/orchestrator.py` | `assemble()` 把 `registry` 和 `arch` 透传给 `build_llm`，让真实 LLM 能拿到工具 schema |
| `.env` | 凭证（API key / base URL / model name） |
| `configs/xiaomi_mimo_smoke.yaml` | smoke-test 用的实验配置（flat tools、状态机 OFF） |
| `scripts/test_xiaomi_mimo.py` | 真实 LLM 端到端 smoke test 脚本 |

### 1.2 既有 pytest 测试（共 14 文件 / 174 用例，**默认跑真实 LLM,无 Key 时退化 mock**）

| 文件 | 用例数 | 介绍 |
|---|---|---|
| `tests/test_world.py` | 9 | MockWorld 的增删改 / snapshot / hash 一致性 |
| `tests/test_tool_base.py` | 7 | `MockTool` 基类自检（必须实现 `intended_entities` 等） |
| `tests/test_tools.py` | 22 | Phase-1 工具（alarm / point / page / widget）业务逻辑 |
| `tests/test_phase2_tools.py` | 36 | Phase-2 工具（graphics / history / scripts / deployment） |
| `tests/test_registry_dispatcher.py` | 14 | 工具注册表 + flat/hierarchical dispatcher |
| `tests/test_state_machine.py` | 7 | 状态机迁移合法性、终态、工具白名单过滤 |
| `tests/test_tool_rag.py` | 11 | Tool RAG（hashing encoder、BM25、rerank、状态过滤） |
| `tests/test_workflow.py` | 18 | YAML workflow catalogue + engine + 触发匹配 |
| `tests/test_resources.py` | 13 | Phase-2 只读 Resources（pages / points / alarms / …） |
| `tests/test_tracer.py` | 3 | JSONL trace 落盘 + idempotent finish |
| `tests/test_config_llm.py` | 5 | YAML 配置加载 + mock LLM 脚本回归 |
| `tests/test_configs.py` | 15 | 8 个 yaml 配置都能 `assemble()`（含 `xiaomi_mimo_smoke.yaml`） |
| `tests/test_e2e.py` | 4 | Phase-1 完整链路：alarm 创建 + 状态机拦截（脚本化 mock_only）|
| `tests/test_phase2_e2e.py` | 10 | Phase-2 各 flag (RAG / Workflow / Resources / SM) 端到端 |

> ⚠️ 自 2026-05 起，`tests/conftest.py` 装有 autouse fixture，会在跑测试时把 `build_llm` 替换成 `tests/_llm_factory.py` 里的工厂：
> - `.env` 里有 `XIAOMI-MIMO_API_KEY` → 所有 `provider="mock"` / `"xiaomi-mimo"` 的配置统一升级为真实 mimo client；
> - Key 不存在或显式 `TEST_LLM_PROVIDER=mock` → 全部退化为 `MockLLM`；
> - 用例打了 `@pytest.mark.mock_only` 的（依赖脚本化 mock 行为或直接断言 `isinstance(..., MockLLM)`）始终走 mock，不受 key 影响。
>
> 因此 `pytest tests/ -q`:
> - 仓库根目录有 `.env` 时 ≈ 90s（带真实 LLM 回归 ~3 个非 mock_only e2e 用例）
> - 显式 `TEST_LLM_PROVIDER=mock venv/Scripts/python -m pytest tests/ -q` ≈ 1s（全 mock）
>
> 详见 §3。

---

## 2. 手动跑测试的三种方式

powershell: 将`PYTHONIOENCODING=utf-8` 改成 `$env:PYTHONIOENCODING='utf-8'`



### 方式 A — 一键 smoke test 脚本（推荐先跑这个）

```bash
cd E:/Agent/Demo
PYTHONIOENCODING=utf-8 venv/Scripts/python scripts/test_xiaomi_mimo.py
```

**这个脚本做什么：**
- 用 `configs/xiaomi_mimo_smoke.yaml` 装配 agent（flat tools / 状态机 OFF / 39 个 atomic tool 全可见）
- 内置一条中文 query：`给反应釜1的TEMP_101点位加一个高温报警,阈值80度,优先级high,ID用alarm_high_temp_101`
- 跑完后打印 trace 摘要 + 每轮 tool call + 每轮 LLM 调用的 token / 延迟 + world hash 变化

**预期输出形态：**

```
[setup] assembling agent from configs/xiaomi_mimo_smoke.yaml
[setup] model = xiaomi-mimo / mimo-v2.5-pro
[setup] atomic tools registered = 39
[setup] state machine = False
[setup] hierarchical = False

[query] 给反应釜1的TEMP_101点位加一个高温报警,...

[result] ---- summary ----
  trace_id       : <uuid>
  total_turns    : 3-5
  terminal_state : ANALYZE_INTENT      ← 状态机关掉了，停在初态正常
  early_terminated: False
  tool_calls     : >=1
  trace_jsonl    : results\xiaomi_mimo_smoke\mimo-v2.5-pro\<hash>\traces.jsonl

[result] ---- tool calls ----
  turn=N selected=create_analog_alarm action=create_analog_alarm ok=True err=OK
           args={"id": "...", "tag": "TEMP_101", "high_limit": 80, "priority": "high"}

[result] ---- llm calls ----
  turn=1  model=mimo-v2.5-pro  in=~9k  out=~500  latency_ms=~5000-9000  stop=tool_use
  ...
  turn=N  ...                                                            stop=end_turn

[result] world_hash sha256:xxx... → sha256:yyy...
[result] world mutated? True

[verdict] PASS — real LLM produced a successful tool call
```

**通过判据**（脚本内部）：`not early_terminated && 至少一个 tool_call 的 result_ok == True` → 进程 exit code = 0。

**可能的非致命现象：**

- 头一两个回合会出现 `SCHEMA_ERROR`（模型把 `high_limit` 当字符串 `"80"` 或 `low_limit` 当字符串 `"None"` 传过来），只要后续回合自我修正、最终有一个 `ok=True` 就算通过。
- 每个回合 input token 可能 9k–10k，因为模型在 thinking mode，且 system prompt 把 39 个工具描述都塞进去了。一次 query 总耗时通常 15–25 秒。

---

### 方式 B — Orchestrator CLI 跑自定义 query

```bash
cd E:/Agent/Demo
PYTHONIOENCODING=utf-8 venv/Scripts/python -m agent.orchestrator \
  --config configs/xiaomi_mimo_smoke.yaml \
  --query "你的查询"
```


可选参数：

- `--golden-id <id>` 给这次跑标一个 ID，写进 trace
- `--no-seed-demo-world` 用空 world（默认会预置 TEMP_101 / PRESS_101 等点位 + reactor_1 设备）
- `--dry-run` 只 assemble、不调 LLM（用来验证配置 + 凭证加载没问题）

**推荐的几条覆盖不同领域的 query**（mock LLM 也支持这些，便于对比）：

| 场景 | query | 预期被调到的 tool |
|---|---|---|
| 报警 | `给 TEMP_101 加个高温报警,阈值 80 度,ID 用 alarm_t101_high` | `create_analog_alarm` |
| 新点位 | `新建一个温度点位 TEMP_999,量程 0~150` | `create_point` |
| 历史查询 | `查 TEMP_101 最近一分钟的历史曲线` | `enable_history` / `query_history` |
| 图形 | `在 p1 页面画一个红色矩形,位置 (50,50) 尺寸 120x80,widget ID 用 r1` | `create_page` 然后 `create_rect`（多半要两步） |
| 校验 | `校验当前项目能不能下装` | `validate_project` |

**预期 CLI 输出形态：**

```
trace_id        : <uuid>
terminal_state  : ANALYZE_INTENT
total_turns     : N
tool_calls      : M
resource_reads  : 0
jsonl           : results\xiaomi_mimo_smoke\mimo-v2.5-pro\<hash>\traces.jsonl
```

每次跑都会**追加**一行到对应 `traces.jsonl`，可用 Python 查看：

```bash
venv/Scripts/python -c "import json; [print(json.dumps(json.loads(l), ensure_ascii=False, indent=2)) for l in open('results/xiaomi_mimo_smoke/mimo-v2.5-pro/<hash>/traces.jsonl', encoding='utf-8')]"
```

---

### 方式 C — 验证凭证 + 配置不出错（不烧 token）

```bash
cd E:/Agent/Demo
venv/Scripts/python -m agent.orchestrator --config configs/xiaomi_mimo_smoke.yaml --dry-run
```

**预期输出：**

```
[dry-run] config=configs/xiaomi_mimo_smoke.yaml  model=mimo-v2.5-pro
[dry-run] domains: ['manage_alarms', 'manage_points', 'manage_pages', 'manage_graphics', 'manage_history', 'manage_scripts', 'deployment']
[dry-run] atomics: 39
[dry-run] tool_rag: False
[dry-run] workflow: 0 workflows
[dry-run] resources: False
```

不调 API、不烧 token，但能验证：`.env` 能被加载、`build_llm` 不抛 `RuntimeError`、注册表完整。

> ⚠️ 注意：`--dry-run` 会触发 `build_llm`，所以如果 `XIAOMI-MIMO_API_KEY` 缺失，这里就会报 `RuntimeError: xiaomi-mimo: missing XIAOMI-MIMO_API_KEY`，可用来快速诊断 `.env` 加载。

---

## 3. 回归测试

每次改完 LLM 适配器后都建议跑一遍，确认两条链路（真实 mimo / mock 退化）都还活着。

### 全量 — 真实 LLM 模式（默认，仓库 `.env` 有 key 时自动启用）

```bash
cd E:/Agent/Demo
venv/Scripts/python -m pytest tests/ -q
```

**预期：`174 passed in ~90s`**。其中 ~3 个非 `mock_only` 的端到端用例真的访问 mimo，其余通过 `@pytest.mark.mock_only` 仍走 `MockLLM` 完成秒级回归。

### 全量 — 强制 mock 模式（不烧 token，秒级）

```bash
cd E:/Agent/Demo
TEST_LLM_PROVIDER=mock venv/Scripts/python -m pytest tests/ -q
```

**预期：`174 passed in ~1s`**。也可通过临时改名 `.env` 来模拟 “无 Key” 的 CI 环境，效果等价。

### 只跑跟 LLM / 配置 / 端到端有关的子集（最相关）

```bash
venv/Scripts/python -m pytest tests/test_config_llm.py tests/test_configs.py tests/test_e2e.py tests/test_phase2_e2e.py -q
```

**预期：`34 passed`**。这是检查 xiaomi-mimo 改动是否最小侵入的关键集合。

### 单个文件

```bash
venv/Scripts/python -m pytest tests/test_configs.py -v
```

`test_configs.py` 里有一个 `test_config_loads_and_assembles[xiaomi_mimo_smoke.yaml]` 项 —— 它确认新 YAML 能被 Pydantic 校验通过并通过 `assemble()`。**有 key 时构造真实 mimo client（不发请求），无 key 时由 autouse fixture 自动退化为 mock**，两种环境都不会爆炸。

---

## 4. 验证 trace 落盘

每次真实 LLM 跑完，看一眼 `results/` 目录：

```bash
ls -la E:/Agent/Demo/results/xiaomi_mimo_smoke/mimo-v2.5-pro/
```

每个子目录是一次 (config × model × config-hash) 组合，里面有：

- `traces.jsonl` — 每条 query 一行 JSON，含 `trace_id` / `tool_calls` / `llm_calls` / `world_snapshots` / 终态 / 早停标志
- `meta.json` — 这次跑的 config 名、model 名、config hash

快速校验 JSONL 合法：

```bash
venv/Scripts/python -c "
import json
from pathlib import Path
for p in Path('results').rglob('traces.jsonl'):
    for line in p.read_text(encoding='utf-8').splitlines():
        json.loads(line)
    print('OK:', p)
"
```

---

## 5. 常见问题速查

| 现象 | 原因 / 处理 |
|---|---|
| `RuntimeError: xiaomi-mimo: missing XIAOMI-MIMO_API_KEY` | `.env` 没在仓库根目录，或没设环境变量。运行前 `ls -la .env` 看一眼 |
| `openai.BadRequestError: Param Incorrect ... reasoning_content in the thinking mode must be passed back` | 如果还看到这个，说明改动没生效；确认 `agent/llm.py` 里 `OpenAICompatibleLLM.call()` 有 `reasoning = getattr(msg, "reasoning_content", None)` 这段 |
| 所有 tool call 都 `SCHEMA_ERROR` / 一直不成功 | 模型把数字传成了字符串。检查 `args_model` 是不是用了 `int \| None` / `float \| None` 这类联合类型——OpenAI tool calling 经常把数字字符串化，目前 Pydantic 严格模式拒收 `"None"`。不影响 smoke 通过判据（只要最终一轮 ok 即可） |
| `terminal_state: ANALYZE_INTENT` 而不是 `DONE` | 配置里 `state_machine.enabled: false`，所以 SM 永远停在初态，正常。要走完整状态推进得另写一个 hybrid 配置 + LLM 显式 `next_state` 协议（超出本次 smoke 范围） |
| `total_turns` 跑到 12 然后 `early_terminated: True` | LLM 反复进入无效 tool call 死循环。看 `tool_calls` 列表里的 `error_code` 找根因，通常是 schema 校验失败 |
| 一次 query 烧 ~40k input tokens | mimo 是 thinking model，且 39 个工具描述都塞 system prompt 了。压缩办法：换 `configs/B_hierarchical_only.yaml` 风格（domain tool 视图，工具数 39→7）但需手动把 model 字段改 `provider: xiaomi-mimo`、`name: mimo-v2.5-pro` |

---

## 6. 推荐的最小回归脚本

每次改动 LLM 相关代码后跑这几条命令：

```bash
cd E:/Agent/Demo

# 1. 强制 mock 回归（不烧 token，秒级）—— 验证 mock 路径
TEST_LLM_PROVIDER=mock venv/Scripts/python -m pytest tests/ -q

# 2. 默认回归（仓库 .env 有 key 时自动跑真实 mimo + mock 混合，~90s）
venv/Scripts/python -m pytest tests/ -q

# 3. 真实 LLM 配置加载体检（不烧 token）
venv/Scripts/python -m agent.orchestrator --config configs/xiaomi_mimo_smoke.yaml --dry-run

# 4. 真实 LLM 端到端 smoke（烧 ~10–40k token，约 20s）
PYTHONIOENCODING=utf-8 venv/Scripts/python scripts/test_xiaomi_mimo.py
```

全绿即认为 xiaomi-mimo 链路健康。
